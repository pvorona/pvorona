# assert

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build assert` to build the library.

## Running unit tests

Run `nx test assert` to execute the unit tests via [Vitest](https://vitest.dev/).
