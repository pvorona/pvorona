# throw-error

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build throw-error` to build the library.

## Running unit tests

Run `nx test throw-error` to execute the unit tests via [Vitest](https://vitest.dev/).
