# reference

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build reference` to build the library.

## Running unit tests

Run `nx test reference` to execute the unit tests via [Vitest](https://vitest.dev/).
